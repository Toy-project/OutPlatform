import React from 'react';

const RegisterForClub = () => (
  <div className="register-container">
    This page RegisterForClub
  </div>
)

RegisterForClub.propTypes = {
};

export default RegisterForClub;
