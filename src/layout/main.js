import React from 'react';
import { Header, MainViewList, Footer } from 'components/';

const main = () => (
  <div>
    <Header />
    <MainViewList />
    <Footer />
  </div>
);

export default main;
